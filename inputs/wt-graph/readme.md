This directory contains edge weights between all vertices on PPDB graph.
Where weight between vertices s and is calculated by:

	                   number_of_vertex(s -> t) 
	         ---------------------------------------------------
	         number_of_vertex(s -> t) + number_of_vertex(t -> s)

